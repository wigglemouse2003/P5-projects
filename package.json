{
  "scripts": {
    "start-0": "live-server --port=8080 --host=localhost creations/attraction-and-repulsion",
    "start-1": "live-server --port=8080 --host=localhost creations/counter",
    "start-2": "live-server --port=8080 --host=localhost creations/dnd-board",
    "start-3": "live-server --port=8080 --host=localhost creations/game-of-life",
    "start-4": "live-server --port=8080 --host=localhost creations/loading",
    "start-5": "live-server --port=8080 --host=localhost creations/maze-generator",
    "start-6": "live-server --port=8080 --host=localhost creations/number-game",
    "start-7": "live-server --port=8080 --host=localhost creations/phi-creature",
    "start-8": "live-server --port=8080 --host=localhost creations/primordial-particle-system",
    "start-9": "live-server --port=8080 --host=localhost creations/survival-algorithm"
  },
  "dependencies": {
    "live-server": "^1.2.1",
    "node": "^13.13.0",
    "open": "^7.0.3",
    "uuid": "^7.0.3"
  },
  "name": "phillip-woolf-p5-creations",
  "version": "1.0.0",
  "main": "app.js",
  "devDependencies": {},
  "author": "Phillip Woolf",
  "license": "ISC",
  "description": "Phillip Woolf's p5.js creations",
  "repository": {}
}
